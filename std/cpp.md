# C / C++ 编码标准

本文件只补充 C / C++ 特有规则。通用工程、安全、数据库和日志要求分别继承 [general.md](general.md)、[security.md](security.md)、[database.md](database.md) 和 [logging.md](logging.md)。

## 适用基线

0. 语言标准、编译器和平台能力以构建配置及部署目标为准，不默认要求升级。
1. C 与 C++ 的错误、资源和 ABI 约定分别定义，不把一种语言的惯例机械套给另一种。
2. 格式化、静态分析和构建工具沿用项目现有配置。

## 接口与类型

0. 命名遵循项目约定；头文件只暴露必要接口，实现细节和私有依赖留在源文件。
1. C++ 使用 `const`、引用、值语义和类型系统表达约束；宏仅用于编译期条件或无法由语言机制替代的场景。
2. 所有权必须可判断：优先值对象、容器和 `unique_ptr`，共享所有权仅在确有共享生命周期时使用。
3. `reinterpret_cast`、裸指针算术和 `unsafe` 式边界必须局部化并说明不变量。
4. C 的缓冲区长度、所有权、可空性和返回码约定必须出现在接口契约中。

## 错误与资源

0. C++ 在异常、`std::error_code`、`std::expected` 或项目错误类型中选择一致策略；C 使用明确返回码与错误输出约定。
1. 空值与失败是不同语义时分别使用 `optional` 与错误类型，不用特殊值混合表达。
2. C++ 通过 RAII 管理文件、锁、内存和句柄；C 使用单一清理路径或封装函数覆盖所有退出分支。
3. 析构函数不得传播异常；不可忽略的结果使用 `[[nodiscard]]` 或等效检查。

## 并发与边界

0. 线程、协程和回调任务必须有明确所有者、停止条件和 join/取消路径；任务组定义失败汇聚与同级任务处置，部分启动失败时停止并 join 已启动任务。
1. 锁的作用域保持可见，不在持锁期间执行未知或可能阻塞的外部代码。
2. 跨 ABI、动态库和 FFI 边界明确内存分配方、释放方、异常与线程约束。

## 工具与测试

0. 启用项目可接受的编译器警告；警告豁免要局部且说明原因。
1. 按项目选择 `clang-format`、`clang-tidy`、编译器静态分析及 Address/Undefined/Thread Sanitizer。
2. 测试框架沿用现有工具；资源、边界、错误和并发代码优先补充 sanitizer 或压力验证，并覆盖阻塞点取消、有界排空、卸载后无回调及支持 ABI 组合的 load/call/unload。

## 常见陷阱

0. 悬空指针、use-after-free、double free 与跨模块分配器不一致。
1. 缓冲区越界、字符串未终止、整数溢出和符号转换。
2. 对象生命周期结束后仍被异步任务或回调捕获。
3. 复制或共享指针掩盖真实所有权，析构顺序依赖全局状态。
