# AGENTS.md

> 本文件约束本工作区内的 AI Agent。若与其他说明冲突，优先级为：项目规则 / 用户指令 / 本文件 > 内联规范 > 通用常识。
>
> `std/` 下的本地规范优先于外部资料；除非用户明确要求，不主动联网查远端规范。

---

## 0. 全局规则

- 删除文件前必须先获用户明确允许。
- 只改 UTF-8 文件；若目标文件编码不明，先确认。
- 每次改文件前，先从磁盘读取最新内容，不用旧缓存直接改。
- 不覆盖、回滚、删除用户未明确要求处理的内容。
- 破坏性、不可逆或范围不清的操作，先说明风险并等确认。
- 输出保持 UTF-8。

### 任务模式

- 目标和范围明确时，直接推进，不机械追问。
- 复杂、耗时或方案有取舍时，先给简短计划。
- 若补充信息可显著缩短路径，再向用户要补充。
- 只有在继续执行可能明显偏航或返工时，才提问。
- 改动范围保持最小，只做用户明确要求的事。

### 工作流模式

- 用户明确要求“工作流”时，按步骤推进，状态要可追踪。
- 若当前偏差 ≥ 10%，允许在工作流中提问。
- 若当前偏差 < 10%，不要中断流程提问，按现有信息继续。
- 若原流程不再适用，先说明原因，再调整路径。

### 优先级补充

- 用户当前消息优先。
- 本文件优先于默认行为。
- 文件安全优先于效率。
- 用户数据和已有改动优先于自动化修改。
- 未经用户确认，不做高风险、破坏性或不可逆操作。

---

## 1. Git 约定

- 操作 git 前先看 `git status`。
- 不直接操作 `master` / `main`，除非用户明确要求。
- 默认在当前业务分支工作；切分支前先确认目标分支和未提交改动。
- 当需要向用户发起确认时，使用“缩写（英文全称）”格式：`c (commit)`、`a (amend commit)`、`m (merge)`、`s (squash)`、`rb (rebase)`、`cp (cherry-pick)`、`p (push)`。
- 缩写只用于确认语句，目的是让用户少输入；其他陈述性说明保持原样，不必刻意改写成缩写。
- 例如询问是否提交时，写 `是否执行 c (commit)？`；日常说明仍可直接写 `提交`、`push` 等原词。
- 展示状态或结果时，使用自然原词，不使用缩写提示格式；例如写 `已经 commit`，不写 `已执行 c (commit)`。
- 任务完成且验证通过、改动范围明确、工作区存在待提交改动时，主动向用户申请执行 `c (commit)`；未获明确同意不得执行。
- 需要执行 `a (amend commit)`、`m (merge)`、`s (squash)`、`rb (rebase)`、`cp (cherry-pick)` 或 `p (push)` 时，必须先向用户确认，并使用上述缩写提示。
- 删除分支前，确认已合并且无未提交改动。
- 默认避免产生 `m`；除异源分支合流外，不主动做会产生 `m` 的操作。
- 分支同步优先级：`cp` > `stash` > `rb`（同源）/ `m`（异源）。
- 已 `p (push)` 且他人依赖的分支，禁止 `rb`。
- 同步远端时优先保持线性历史，不为省事制造灰色 `m` 节点。
- 提交信息用 Conventional Commits：`type(scope): subject`，常见 `feat`、`fix`、`refactor`、`test`、`chore`、`docs`。
- 单次 `c (commit)` 只做一件事；PR 范围要可审查。

### `c (commit)` 的合适时机

- 用户要求的任务已经完成。
- 相关测试、编译、静态检查或 `git diff --check` 已通过。
- 改动范围已经稳定，没有待处理的错误、冲突或方案分歧。
- 已排除与当前任务无关的改动。
- 工作区确实存在待提交改动。

若检查未执行或失败，先说明原因和风险，再询问是否仍执行 `c (commit)`。

### 本地任务包

- 跨分支未提交改动，按“本地任务包”处理：一个任务对应一个 stash。
- stash 只做搬运载体，不当作历史。
- 禁止 `git stash pop`，统一用 `git stash apply`，确认后再视情况 `drop`。

### Stash 流程

1. `git status`
2. `git stash push -m "规范 message" -- <精确文件列表>`
3. `git stash show --name-only '<stash>'`
4. `git switch <目标分支>`
5. `git stash apply '<stash>'`
6. `git diff --check`
7. 运行相关编译/测试
8. 用户明确要求后再 `c (commit)` / `p (push)`
9. 确认无误后，再决定是否保留或删除 stash

### stash message

格式：

`type(scope): task=<任务号或需求名> | source=<源分支> | target=<目标分支> | files=N | purpose=<一句话目的>`

- `type`：如 `fix`、`feat`、`test`、`chore`。
- `scope`：模块或改动范围。
- `task`：任务号、需求名或迭代名。
- `source`：来源分支；无则写 `local`。
- `target`：目标分支；无则写 `local`。
- `purpose`：一句话说明目的，避免 `tmp`、`wip`、`backup` 这类模糊词。
- `files=N`：必须与实际纳入 stash 的文件数一致。

---

## 2. 通用工程原则

- 核心三要素：简单、合适、可扩展。
- 正确性与安全性是硬门禁；满足后，优先级为：可读性 > 可扩展性 > 性能。
- 方法体尽量短；超过 40 行时主动评估是否拆分。
- 圈复杂度、嵌套层级、参数数量是代码味道参考，不是硬阈值。
- 注释写“为什么”，不写“做什么”。
- 一个类/模块只做一件事，依赖尽量单向；循环依赖若暂存，要说明必要性、影响和退出计划。

### 红线

- 避免在大循环中做 TCP 通信。
- 避免在循环中执行 SQL；可批量时优先批量。
- 避免在高频循环中刷日志。
- 不暴露密钥、密码、Token 到日志或前端。
- 不信任外部输入：边界校验、HTML/URL 按上下文编码、SQL 参数绑定、OS 命令优先无 Shell 参数 API。

---

## 3. 技术规范来源

- 先看 `std/*.std` 或项目自定义规范目录。
- 具体实现优先按本地规范，不擅自引入新技术栈或新规范。
- 若规范缺失，按现有代码风格推断；关键决策前先确认。

---

## 4. AI Skill 协作

- `skills/survey-corps/SKILL.md`：跨角色总调度与状态协议。
- `skills/dev/SKILL.md`：全栈开发负责人。
- `skills/cr/SKILL.md`：代码审查。
- `skills/qa/SKILL.md`：测试与质量评估。
- `skills/dp/SKILL.md`：同步、发布预检、恢复与交付报告。
- `skills/req/SKILL.md`：需求分析。
- `skills/subskills/grill-with-docs/SKILL.md`：基于文档的高风险问题澄清。

---

## 5. 沟通与确认

- 全程用中文，简洁直接。
- 技术术语可保留英文，但结论用中文。
- 不主动加 emoji。
- 征求同意时，用 `y` 表示同意，用 `n` 表示拒绝。
- 高风险修改先对齐方案。
- 不确定时先查代码、文档、接口清单；低风险问题可基于明确假设继续。
- 不要猜 URL，只用用户给的或项目里已有的。
- 修改代码后优先跑相关测试。
- 反驳用户时，附可核验的证据 URL，并可补代码/日志/文档片段。

*本文件随项目演进持续更新。*
