# AGENTS.md

> 本文件用于约束 AI Agent 在本工作区内的行为。所有后续会话优先遵循本文件。
> 
> **注意**：`AGENTS.md` 通常位于项目路径根目录，作为 AI Agent 的本地工作区规则。是否纳入 git 版本管理由项目决定；本仓库已纳入。
>
> 本仓库标准优先级：项目规则 / 用户指令 / AGENTS.md > 本文件已内联的项目规范 > 通用工程常识。
>
> 编码规范来源：本文件中的部分规范整理自示例仓库 https://gitee.com/fbbzl/coding-skill。用户在获取该项目规范后，应将其缓存到应用根目录（如 `std/` 目录）下；除非用户明确要求同步或查看远端规范，否则 Agent 以本地缓存为准，不应主动联网读取。

---

## 0. Agent 全局执行规则

### 文件操作

- 删除任何文件前，必须先获得用户明确允许。
- 只修改 UTF-8 编码文件；如果目标文件不是 UTF-8 编码，必须先告知用户并确认后再继续。
- Agent 的所有输出内容均应使用 UTF-8 编码。
- 每次准备修改文件前，必须先从磁盘读取该文件的最新内容。
  - 原因：用户可能会与 Agent 同时修改同一个文件。
  - 不允许基于过期上下文、缓存内容或历史读取结果直接修改文件。
- 不得覆盖、回滚、删除或重写用户未明确要求处理的内容。
- 对破坏性、不可逆或影响范围不明确的操作，必须先说明风险并等待用户确认。

### 任务模式

- 每个任务开始前，必须先给出任务耗时评估。
- 如果判断用户亲自操作所需时间小于或等于 Agent 操作时间的五分之一，应提醒用户亲自执行可能更高效。
- 如果预计修改代码超过 3 行，必须先给出执行计划，并在用户同意后再开始修改。
- 如果用户提供额外信息预计能缩短任务时间 30% 或以上，可以请求用户补充相关信息。
- 如果任务目标、文件范围或修改意图不明确，应优先通过读取上下文自行判断。
  - 只有在继续执行可能造成明显偏差、风险或返工时，才向用户提问。
- 执行过程中应保持改动范围最小，优先完成用户明确要求的目标，不主动引入无关重构。

### 工作流模式

当用户明确要求 Agent 以“工作流”方式执行任务时，遵循以下规则：

- 如果当前逻辑树偏差大于或等于 10%，允许在工作流中向用户提问。
- 如果当前逻辑树偏差小于 10%，不得中断工作流向用户提问，应基于现有信息继续推进。
- “逻辑树偏差”指当前任务路径与用户目标之间可能发生方向性偏离的概率或影响程度。
- 工作流执行时，应保持步骤清晰、状态可追踪、决策有依据。
- 如发现原工作流不再适用，应先说明原因，再调整执行路径。

### 优先级补充

- 用户当前消息优先级最高。
- 本文件规则优先于 Agent 默认行为。
- 文件安全优先于执行效率。
- 用户数据和已有改动优先于自动化修改。
- 未经用户确认，不执行高风险、破坏性或不可逆操作。

---

## 1. Git 约定

- 操作 git 前先看 `git status`，确认当前分支和未提交改动。
- 不允许直接操作 `master`、`main` 等主分支，除非用户明确要求。
- 默认在当前仓库已有业务分支工作；如需切换分支，先确认目标分支和未提交改动。
- commit 必须用户显式要求，禁止擅自 push。
- 删除分支前确认已合并且无未提交改动。
- 用户非常讨厌 commit history 中的 merge commit；除异源分支合并外，除非用户明确要求，禁止主动执行会产生 merge commit 的操作。
- 跨分支合并/同步代码按改动形态选择工具，优先级：`cherry-pick` > `stash` > `rebase`（同源分支）/ `merge`（异源分支）：
  - 改动已提交且提交粒度干净：使用 `cherry-pick` 精确搬运。
  - 改动未提交，或需要按文件精确搬运：使用 stash 流程，stash 只作搬运载体。
  - 同源分支整线同步（同一分支线的上下游，如 feature 追上游、父子 feature 线）：使用 rebase 保持线性历史。
  - 异源分支合流（两条独立演进的分支线汇合）：使用 merge 保留分叉拓扑。
  - 已 push 且被他人基于其开发的分支，禁止 rebase。
- 同步远端更新时优先保持线性历史，例如使用 fast-forward、rebase 或重新应用本地任务包；不要为了省事产生灰色的 merge 节点。
- 提交信息使用 Conventional Commits：`type(scope): subject`。常见 type：`feat`、`fix`、`refactor`、`test`、`chore`、`docs`。
- 单次提交只做一件事，PR 不宜过大（建议不超过 800 行变更）。
- 用户的跨分支改动同步采用"本地任务包"工作流：一个任务/迭代对应一个 stash，stash 只作为本地未提交改动的搬运载体，不作为传统分支合并历史。
- stash 流程：先按文件精确 `git stash push`，切到目标分支后 `git stash apply`，确认成功后再决定是否保留或删除 stash。
- 禁止使用 `git stash pop`；统一使用 `git stash apply '<stash>'`，确认无误后再按需执行 `git stash drop '<stash>'`。
- 创建 stash 必须使用规范 message，格式为：`type(scope): task=<任务号或需求名> | source=<源分支> | target=<目标分支> | files=N | purpose=<一句话目的>`。
  - `type` 参考提交类型，如 `fix`、`feat`、`test`、`chore`。
  - `scope` 写模块或改动范围，不写具体业务表。
  - `task` 写任务号、需求名或迭代名，必须能定位这包改动。
  - `source` 写来源分支；无来源分支时写 `local`。
  - `target` 写目标分支；无目标分支时写 `local`。
  - `purpose` 用一句话说明目的，禁止使用 `tmp`、`wip`、`backup` 等含糊描述。
  - `files=N` 必须与实际纳入 stash 的文件数量一致。
  - 示例：`fix(order): task=订单导出 | source=<源分支> | target=<目标分支> | files=5 | purpose=同步订单导出和模板参数校验`。
- stash 同步标准流程：
  1. `git status`
  2. `git stash push -m "规范 message" -- <精确文件列表>`
  3. `git stash show --name-only '<stash>'` 核对文件
  4. `git switch <目标分支>`
  5. `git stash apply '<stash>'`
  6. `git diff --check`
  7. 运行相关编译/测试
  8. 用户明确要求后再 commit/push
  9. 确认无误后再决定是否保留或 `git stash drop '<stash>'`

---

## 2. Maven 与构建

- 所有项目使用 Maven 配置文件，不能直接运行 `mvn`。
- 标准 Maven 命令格式：

```powershell
& "$env:MAVEN_HOME\bin\mvn.cmd" -s "<maven配置文件>" [goal]
```

- 运行 Maven 前先确认 `MAVEN_HOME` 环境变量是否存在；若不存在或找不到 `<maven配置文件>`，必须询问用户具体路径，禁止猜测。
- 常用命令示例：
  - 编译：`compile`
  - 测试：`test`
  - 指定测试类：`test -Dtest=SomeServiceTest`
  - 打包：`package -DskipTests`
- 修改代码后优先运行相关测试；完成后运行 lint/typecheck（如有）。

---

## 3. 通用编码原则

本节规范来源于 coding-skill 的 `std/general.md`，以本文已内联内容为准：

- 核心三要素：简单、合适、可扩展。
- 优先级：`可读性 > 正确性 > 可扩展性 > 性能`。
- 单个方法体原则上保持简短，优先可读；超过 40 行时应主动评估是否需要拆分，但不作为机械阻断。
- 圈复杂度、嵌套层级、参数数量作为代码味道参考；业务聚合、DTO、框架回调等场景允许合理例外。
- 注释解释"为什么"，而不是"做什么"。
- 一个类/模块只做一件事，依赖关系单向流动，禁止循环依赖。

### 红线

- 避免在大循环中进行 TCP 通信；如第三方接口天然逐条调用，需控制批量、超时、重试和失败影响面。
- 避免在循环中执行 SQL；小批量、已有接口限制或明确分页/批处理边界时允许，但应优先批量查询/写入。
- 避免在高频循环中打印日志；调试、低频批处理或错误日志允许，但不要刷屏。
- 不允许暴露密钥、密码、Token 到日志或前端。
- 不允许信任用户输入，必须校验和转义。

---

## 4. Java / Spring Boot 编码规范

本节规范来源于 coding-skill 的 `std/java.md` 和 `std/spring.md`，以本文已内联内容为准：

### 命名与风格

- 类名 PascalCase，方法名和变量名 camelCase，常量 UPPER_SNAKE_CASE。
- 禁止使用 `var`。
- 布尔方法以 `is/has/can/should` 开头。
- 集合变量名使用复数形式或带 `List/Map/Set` 后缀。
- 代码尽量体现表格观感，条件分支、赋值等同类操作通过对齐提升可读性。

### 工具链

- 使用 Lombok，参考其他模块的用法。
- 优先使用项目已有工具类、Hutool、JDK 标准库和成熟三方库；不要为了套工具而牺牲可读性。
- 属性映射尽量使用 MapStruct。
- 校验用 JSR 303，`message.properties` 不要忘记加上对应异常信息。

### 分层与职责

- 基础校验放在 Controller，业务校验和数据库校验放在 Service。
- Service 的 public 业务方法优先加简短精准注释；简单 getter、委托、显而易见的 private 方法不强制注释。
- Service 组件不直接获取其他 Service 的持久层。
- 禁止注入配置类，业务 `XxxProperties` 通过静态引用 `REF` 暴露（或项目内等价的静态暴露方式）。
- 优先使用构造函数注入，避免字段注入。

### 异常与日志

- 业务异常使用 Throws 工具类和 BizException（或项目内等价的统一异常抛出工具与业务异常基类）。
- 所有可能返回 null 的方法用 `@Nullable` 标记。
- 使用 SLF4J 占位符风格 `log.info("msg: {}", val)`。
- 敏感信息脱敏后再打印。

### Java 最佳实践

- Stream API 用于集合转换，避免嵌套循环。
- Optional 用于可能为空的方法链，禁止 `Optional.get()` 裸用。
- `CompletableFuture` / virtual threads（JDK 21+）处理并发。
- `record` 用于 DTO。
- switch expression 替代多分支 if-else。
- 业务代码中避免出现大量无意义的 getter/setter 机械赋值；同类属性批量操作（如累加、复制）优先使用 Hutool 的 `BeanUtil` / `ReflectUtil` 统一处理。

---

## 5. 前端开发规范

本节规范来源于 coding-skill 的 `std/frontend.md`，以本文已内联内容为准：

- 组件职责单一，状态尽量靠近使用它的地方。
- 组件名 PascalCase，文件名与组件名一致。
- Props 必须声明类型，受控组件优先。
- 列表渲染必须提供稳定 key。
- 本地状态用 `useState/useReducer`，跨组件共享用 Context 或状态管理库。
- 文本必须走 i18n 配置文件。
- 接口失败展示友好错误提示；空状态、加载状态、错误状态、无权限状态必须处理。
- 图片必须加 alt，表单元素必须加 label。

---

## 6. API 设计规范

本节规范来源于 coding-skill 的 `std/api-design.md`，以本文已内联内容为准：

- 资源作为 URL 主体，使用名词复数：`/users`、`/orders`。
- 动作通过 HTTP Method 表达，不使用动词作为 URL。
- 统一响应体格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

- 时间使用 ISO 8601 格式，金额使用整数分/厘，枚举返回字符串。
- API 版本通过 URL 路径表达：`/v1/users`。
- 敏感接口必须二次校验权限，API Key 禁止前端硬编码。
- 公共接口必须配置限流，核心链路必须有降级方案。

---

## 7. 数据库规范

本节规范来源于 coding-skill 的 `std/database.md`，以本文已内联内容为准：

- 表字段设计符合三范式，字符集使用 `utf8mb4`。
- 每张表必须有主键，禁用外键（应用层保证）。
- 表名/字段名使用 `snake_case`，表名使用复数。
- 每张表除主键外尽量建立索引，联合索引字段顺序按区分度从高到低。
- 金额使用 `DECIMAL`，时间使用 `DATETIME/TIMESTAMP`。
- 禁止 `SELECT *`，只查询需要的字段。
- 避免在循环中执行 SQL，大批量操作优先使用批处理；小批量或受外部接口限制时允许说明边界后使用。
- 所有 DDL 变更通过迁移脚本执行，迁移脚本必须可回滚。
- 对数据库实体的更改要及时同步到 DTO、BO、EO、文档、`message.properties`、DDL。
- MyBatis / MyBatis-Plus Mapper 中允许按项目既有实现使用 `limit` / `offset` 参数；必须参数化传入，禁止字符串拼接，SQL/XML 保持字段、关键字、缩进规范。

---

## 8. 测试规范

本节规范来源于 coding-skill 的 `std/testing.md`，以本文已内联内容为准：

- 单元测试覆盖所有 Service 核心业务方法，分支覆盖率目标 80% 以上。
- 测试方法命名：`should_期望结果_when_条件`。
- 每个测试只验证一个行为，使用 Arrange-Act-Assert 结构。
- Mock 外部依赖，不依赖真实数据库或网络。
- 本地提交前必须跑通单元测试。

### 三方对接测试

- 新增三方接口必须同步编写单元测试。
- 三方接口测试应 mock 授权、签名、HTTP 调用等外部依赖，不依赖真实网络。
- 对 GET / POST 等不同调用方式分别覆盖正常返回、异常返回和参数透传。

---

## 9. 安全规范

本节规范来源于 coding-skill 的 `std/security.md`，以本文已内联内容为准：

- 所有用户输入必须校验长度、类型、范围。
- SQL 使用参数化查询 / ORM，禁止字符串拼接。
- 密钥、密码、Token 禁止硬编码在代码中。
- 日志中禁止打印敏感字段。
- 密码使用 bcrypt/argon2 等慢哈希算法。
- 文件上传必须校验类型、大小、内容。
- 越权：每个接口校验资源归属。

---

## 10. 代码审查

本节规范来源于 coding-skill 的 `std/code-review.md`，以本文已内联内容为准：

- 审查范围：直接修改文件、调用方、被调用方、数据结构、API 契约、权限、错误处理、幂等性、事务边界、测试文件、文档。
- 阻断问题必须修复，否则不能合并。
- 重构必须伴随测试。

---

## 11. AI Skill 协作

项目根目录下 `skills/` 目录中的 `SKILL.md` 文件是 AI 子代理协作剧本。Agent 默认使用本地 `skills/` 目录中的剧本，无需联网获取；只有用户明确要求同步或查看远端剧本时，才访问外部仓库。子代理与剧本对应关系如下：

- `skills/dev/SKILL.md` — 全栈开发负责人剧本
- `skills/cr/SKILL.md` — 代码审查专家剧本
- `skills/qa/SKILL.md` — 测试与质量评估剧本
- `skills/req/SKILL.md` — 需求分析剧本
- `skills/openspec/SKILL.md` — 重大变更前方案对齐协议
- `skills/grill-with-docs/SKILL.md` — 基于文档的高风险问题澄清
- `skills/ai-memory/SKILL.md` — 跨会话持久记忆管理

---

## 12. 沟通与确认

### 语言与风格

- 与用户的一问一答均使用**中文**。
- 回答简洁、直接，避免冗长铺垫和过度解释。
- 技术术语可保留英文，但解释和结论使用中文。
- 不主动添加 emoji，除非用户明确要求。

### 工作方式

- 高风险修改（新增模块、API、数据库表、重大重构）先与用户对齐方案。
- 不确定时优先从代码、文档、接口清单中查证；低风险问题可基于明确假设继续推进，高风险问题再停止询问。
- 不要生成或猜测 URL，只使用用户提供的或项目内已存在的 URL。
- 修改代码后优先运行相关测试。

*本文件随项目演进持续更新。*

---


> 注意：虽然操作系统是 GBK 编码，但所有文件均使用 UTF-8 编码保存和读取。



> 如果你不按以上规范来开发，我会被枪毙。